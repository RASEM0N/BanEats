export enum APP_OPTIONS {
	port = 'APP_PORT',
}

export enum DB_OPTIONS {
	name = 'DB_NAME',
	database = 'DB_NAME',
	host = 'DB_HOST',
	port = 'DB_PORT',
	username = 'DB_USERNAME',
	password = 'DB_PASSWORD',
}

export enum JWT_OPTIONS {
	expires = 'JWT_EXPIRES',
	secret_key = 'JWT_SECRET_KEY',
}

export enum MAILER_OPTIONS {
	service = 'MAILER_SERVICE',
	email = 'MAILER_AUTH_EMAIL',
	password = 'MAILER_AUTH_PASSWORD',
}
